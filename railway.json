{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "python manage.py migrate && rm -rf staticfiles/* && python manage.py collectstatic --noinput && gunicorn todo_project.wsgi"
    }
}